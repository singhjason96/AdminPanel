<h1>Users#index</h1>
<%=link_to "New User", new_user_path %>
<h4>Users</h4>
<% @users.each do |user| %>
    <div>
    <%=link_to user.first_name + ' ' + user.last_name, user_path(user.id) %>
    </div>
    <% end %>

    